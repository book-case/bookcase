const router = require('express').Router();

router.get('/:id/:volume', (req, res, next) => {
	//TODO
});

router.get('/:id/:volume/:page', (req, res, next) => {
	//TODO
});

module.exports = router;
